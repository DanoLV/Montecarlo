{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "FMake",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${fileDirname}",
            }
        }
    ]    
}

